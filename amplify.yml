version: 1
frontend:
  phases:
    build:
      commands:
        - echo "const CONFIG = { LAMBDA_URL: '${LAMBDA_URL}' };" > config.js
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - config.js
  cache:
    paths: []
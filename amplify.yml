version: 1
frontend:
  phases:
    build:
      commands:
        - echo "const CONFIG = {};" > config.js
  artifacts:
    baseDirectory: /
    files:
      - index.html
      - config.js
  cache:
    paths: []
